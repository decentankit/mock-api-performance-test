# Mock API + Performance Test Suite

This repository contains two main components:

- **mock-api/** → A lightweight backend server used to simulate the API endpoints.
- **performance-test/** → k6 scripts written in TypeScript to test performance against the mock API.

---

